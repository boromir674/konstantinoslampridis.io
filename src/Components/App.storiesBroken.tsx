import App from "./App";

export default {
  component: App,
  title: "App",
  tags: ["autodocs"],
};

// Light Mode Colors
export const LightMode = {
    args: {
        // same interface as the props of the Component
    },
};

// export const DarkMode = {
//     args: {
//         // same interface as the props of the Component
//         ...LightMode.args
//     },
// };
